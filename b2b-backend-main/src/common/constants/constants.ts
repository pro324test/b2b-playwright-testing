// filepath: src/auth/constants.ts

// Constants to be used across the application
// JWT related constants don't contain actual secrets anymore
export const jwtConstants = {
  // Placeholder for JWT constants that might be needed
  // Actual secret is now stored in environment variables and accessed via ConfigService
};